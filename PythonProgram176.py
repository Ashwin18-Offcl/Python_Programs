"""Define a properly that must have the same value for every class instance (object) Define a class attributes 'color' with a default value while
I.e Every Vehicle should be white.""
"""
class Vehicle:
    Color ="White"
    def_init_(self,name, max_speed,mileage):
    self.name = NameError
    self.max_speed = max_speed
    self.mileage = mileage
class Bus(Vehicle):
    pass
class Car(Vehicle):

School_bus=Bus("Volvo",120,23)
print(Maruti.name,Maruti.Color,Maruti.max_speed,Maruti.mileage)
